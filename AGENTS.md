## 项目定位
本仓库是基于 xv6-RISC-V 的操作系统课程设计项目。
项目主题：静态优先级调度与 Aging 防饥饿机制。
团队共 5 人，目标是工作量适中、功能稳定、贡献清晰。
Agent 开始工作前必须先阅读根目录下的 `plan.md`。
若发生冲突，优先级为：用户当前明确指令 > 本文件 > `plan.md` > 仓库现有习惯。

如果开发过程中遇到问题，请在修复后记录到 @docs/bugs.md

## 项目范围

允许实现：
- 进程优先级字段；
- 静态优先级调度；
- 同优先级基本公平；
- Aging 防饥饿；
- `setpriority`、`getpriority`；
- 简单用户命令；
- `prioritytest`、`agingtest`；
- 必要的测试、日志和文档。
未经用户明确要求，不得增加：
- MLFQ、CFS、红黑树、复杂优先级队列；
- 图形化监控、复杂进程监控；
- 动态 I/O 优先级；
- 与本课题无关的文件系统、内存或网络功能；
- 大规模 xv6 重构。

## 五人职责
成员 1：负责进程优先级数据结构、默认值、fork 继承和字段重置。
成员 2：负责静态优先级调度和同优先级公平。
成员 3：负责 Aging、等待时间累计和防饥饿逻辑。
成员 4：负责优先级系统调用和用户态命令。
成员 5：负责测试程序、集成验证和测试日志。
Agent 只完成当前分配的模块。
不要因为“顺手”而实现其他成员的功能。
如必须跨模块修改，应保持最小范围并明确说明。

## 开发原则
优先采用“小修改、可编译、可运行、再继续”的方式。
修改核心代码前必须先阅读仓库现有实现。
不要凭记忆猜 xv6 接口。
不要进行无关代码格式化。
不要大范围移动、重命名或重构原始代码。
不要为了代码更“高级”而引入不必要的抽象。
完成既定需求后停止扩展功能。

## 优先级约定
默认采用：
- `0` 为最高优先级；
- `31` 为最低优先级；
- 默认值为 `10`；
- fork 后子进程继承父进程优先级。
非法优先级必须统一拒绝或处理。
Aging 不得让优先级越过合法范围。
不同模块不得自行改变以上语义。

## 调度器约束
不得破坏 xv6 原有进程状态转换规则。
不得随意删除 `p->lock`。
不得假设系统只有一个 CPU。
只能调度 `RUNNABLE` 进程。
修改进程状态和调度字段时必须注意锁保护。
同优先级进程不能长期固定只运行一个进程。
实现必须保持简单，保证负责人能够解释。

## Aging 约束
Aging 仅用于减少静态优先级调度中的饥饿。
等待中的 `RUNNABLE` 进程可累计 `wait_ticks`。
达到阈值后提升优先级，并按统一规则重置等待状态。
阈值应使用命名常量，不使用难以解释的魔法数字。
不得把 Aging 擅自扩展成 MLFQ 或复杂动态优先级算法。

## 系统调用约束
新增系统调用必须遵循 xv6 原有结构。
不得覆盖已有系统调用编号。
必须检查 PID 是否存在。
必须检查优先级参数是否合法。
用户态声明、调用入口和内核实现必须一致。
不得顺便修改与任务无关的系统调用。

## 测试要求
代码能编译不代表任务完成。
内核修改后至少执行构建测试。
环境允许时应启动 QEMU 验证。
最终版本至少验证：
- xv6 正常启动；
- `usertests`；
- `prioritytest`；
- `agingtest`；
- 优先级查询和设置功能。
还应覆盖：
- 相同优先级；
- 不同优先级；
- 边界优先级；
- 非法优先级；
- fork 继承；
- Aging 生效；
- 进程退出后系统稳定。
禁止伪造测试通过、终端输出、截图、性能数据或测试日志。
如果无法执行测试，必须明确说明“未在当前环境执行”。

## Git 规则
一个 commit 只解决一个清晰问题。
提交信息应能看出模块和目的。
推荐使用 `feat`、`fix`、`test`、`docs` 等前缀。
禁止使用 `update`、`修改`、`final2` 等无意义提交信息。
禁止为了增加贡献数量制造无意义 commit。
禁止修改作者信息冒充其他成员。
不得擅自重写、压缩或删除其他成员的提交历史。

## Agent 输出要求
执行任务前先说明准备修改的范围。
完成后说明实际修改了什么。
只报告真正执行过的测试。
发现未解决问题时必须明确指出。
不要隐瞒失败、回归或未验证行为。
不要自行做超出 `plan.md` 的架构决定。

## 完成标准
功能符合 `plan.md`。
修改范围与负责人边界一致。
代码可以编译。
能测试的内容已经测试。
没有故意忽略严重回归。
Git 提交清晰可追踪。
AI 使用能够说明来源和人工验证情况。
负责人能够解释自己的实现。
项目达到既定功能后，优先修 bug、补测试和整理文档，不继续堆功能。

